// For now, system queries are not needed as most system info is fetched from os-utils and systeminformation packages.
// If you later store or fetch system stats from the database, add those queries here.
